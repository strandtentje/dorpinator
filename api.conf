{
	instances = {
		doc = Template(f"apidoc.html");

		json = MySqlQuerier(f"GetImage.sql", credset = "plaatsnaam") {
			single_branch = Template(f"skeleton.json") {
				plaatsnaam_branch = MySqlQuerier(f"GetPlaatsnaam.sql", credset = "plaatsnaam") {
					single_branch = Template(f"ViewNaam.json");
				};
				provincie_branch = MySqlQuerier(f"GetProvincie.sql", credset = "plaatsnaam") {
					single_branch = Template(f"ViewNaam.json");
				};
			};
		};			
	};
}