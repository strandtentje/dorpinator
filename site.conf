{
	instances = {
		credentials = Remember("plaatsnaam", 
			host = "localhost", db = "plaatsnaam", 
			user = "plaatsnaam", pass = "plaatsnaam", 
			pool = true, dbtype = "mysql"
		);

		begin = SiteSubsection() {
			main_branch = Template(f"skeleton.html") {
				image_branch = SQL(f"GetImage.sql", credset = "plaatsnaam") {
					single_branch = Template(f"ViewNaam.html");
				};
				plaatsnaam_branch = SQL(f"GetPlaatsnaam.sql", credset = "plaatsnaam") {
					single_branch = Template(f"ViewNaam.html");
				};
				provincie_branch = SQL(f"GetProvincie.sql", credset = "plaatsnaam") {
					single_branch = Template(f"ViewNaam.html");
				};
			};
			static_branch = Fileserver(rootpath = f"static", allowedmimetypes = {
				css = "text/css";
				woff = "application/x-font-woff";
				jpg = "image/jpg"
			});
		}; 
	};
}